import React from "react";
import { Pool } from "../../../lib";

type DepositProps = {
  pool: Pool;
};

export function Deposit({ pool }: DepositProps) {
  return (
    <>
      <p>Deposit</p>
    </>
  );
}
