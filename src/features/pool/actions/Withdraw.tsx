import React from "react";
import { Pool } from "../../../lib";

type WithdrawProps = {
  pool: Pool;
};

export function Withdraw({ pool }: WithdrawProps) {
  return (
    <>
      <p>Withdraw</p>
    </>
  );
}
