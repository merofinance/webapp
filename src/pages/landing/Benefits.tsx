import React from "react";
import styled from "styled-components";

const StyledBenefits = styled.div`
  width: 100%;
  margin: var(--section-margin);
`;

const Benefits = () => {
  return <StyledBenefits>benefits</StyledBenefits>;
};

export default Benefits;
