import React from "react";
import styled from "styled-components";

const StyledGetStarted = styled.div`
  width: 100%;
  margin: var(--section-margin);
`;

const GetStarted = () => {
  return <StyledGetStarted>get started</StyledGetStarted>;
};

export default GetStarted;
