import React from "react";
import styled from "styled-components";

const StyledSupportedBy = styled.div`
  width: 100%;
  margin: var(--section-margin);
`;

const SupportedBy = () => {
  return <StyledSupportedBy>supported by</StyledSupportedBy>;
};

export default SupportedBy;
