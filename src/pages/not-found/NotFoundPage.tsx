import React from "react";
import styled from "styled-components";

const StyledNotFoundPage = styled.div`
  width: 100%;
  display: flex;
  margin: auto;
  flex: 1;
`;

const NotFoundPage = () => {
  return <StyledNotFoundPage>meow</StyledNotFoundPage>;
};

export default NotFoundPage;
